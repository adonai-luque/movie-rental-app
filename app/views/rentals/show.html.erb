<h3>Rental detail</h3>

<h6>Movie:</h6>
<p><%= @rental.movie_id %></p>

<h6>Email:</h6>
<p><%= @rental.user_id %></p>

<%= link_to "Edit", edit_rental_path(@rental), class: "btn btn-outline-dark" %>
<%= link_to "Delete", rental_path(@rental), method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-outline-danger"  %>
<%= link_to "Back to #{$new_rental_referer || "rentals"}", "/#{$new_rental_referer || "rentals"}", class: "btn btn-outline-dark" %>